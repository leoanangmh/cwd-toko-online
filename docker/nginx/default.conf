# This file is overwritten at container startup by entrypoint.sh
# It exists only so nginx can start during image build validation
server {
    listen 80 default_server;
    server_name _;
    return 503;
}